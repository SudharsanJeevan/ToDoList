# ToDoList
Think of React like a smart whiteboard:  You write tasks on it  When you erase or add something, the board automatically shows the changes  You don't have to redraw the whole board, just the parts that changed
